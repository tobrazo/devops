apiVersion: v1
kind: Service
metadata:
  name: centrifugo
spec:
  type: {{ .Values.service.type }}
  selector:
    app: centrifugo
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8000
