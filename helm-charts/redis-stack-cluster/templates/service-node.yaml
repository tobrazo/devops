apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: redis-cluster
  ports:
    - port: {{ .Values.service.port }}
      name: client
    - port: {{ .Values.service.gossipPort }}
      name: gossip
